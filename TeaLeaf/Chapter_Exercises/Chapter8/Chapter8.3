# Required:
# Write a program that allows user to enter as many lines of info as they want,
# And stops when the user enters an empty string, (presses 'Return' without an entry)
# Then repeats the words back to the user in alphabetical order.
# An empty 'Return' on the first line should exit the program.

# Pseudo Code:

#Define the array
# Check array for blank entry, then end program, if blank
  # Prompt user to enter values
  # Sort Array
  # Display array to screen

puts 'hello'
user_array = []
user_array.each do |entry|
  gets.chomp entry
  #if entry = null
  #  break
  #end
  puts user_array.sort.join (', ')
end

puts 'Enter some words. Return twice when you\'re done.'
gets.chomp.to_s user_array

user_array = Array.new
while user_array != 'n'
  user_array << gets.chomp
  puts 'What else?'
end
puts user_array.sort



